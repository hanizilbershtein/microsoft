import React from 'react'

const SectionTitel = ({title,piska}) => {
    return (
        <div className="title">
           <h4>{title}</h4>
           <p>{piska}</p> 
        </div>
    )
}

export default SectionTitel
